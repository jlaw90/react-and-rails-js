import { type ReactElement, type ComponentType } from 'react';
import type { ServerAdapter } from '..';
declare const ReactServerAdapter: ServerAdapter<string | ComponentType, ReactElement>;
export default ReactServerAdapter;
